# Relay WebSocket URL (wss:// for production, ws:// for local dev)
# For mobile builds, this must point to a publicly reachable server (not localhost)
# For LAN testing on a real device: ws://192.168.x.x:3001/ws
VITE_RELAY_URL=ws://localhost:3001/ws
